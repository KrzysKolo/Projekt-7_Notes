class MyNots {
  constructor() {
    this.UiSelectors = {
      add: ".add",
      deleteAll: ".delete-all",
      deleteNote: "delete-note",
      note: ".note",
      save: ".save",
      cancel: ".cancel",
    };
  }
  initializeMyNotes() {
    this.addBtn = document.querySelector(this.UiSelectors.add);
    this.addBtn = document.querySelector(this.UiSelectors.add);
    this.addBtn = document.querySelector(this.UiSelectors.add);
    this.addBtn = document.querySelector(this.UiSelectors.add);

    this.addBtn = document.querySelector(this.UiSelectors.add);
    addEventListeners();
  }
  addEventListeners() {}
}
